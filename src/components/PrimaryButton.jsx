import React from 'react';

const PrimaryButton = ({children}) => {
    return <span  className="py-2 px-28 bg-blue-600 text-xl font-semibold rounded-md ">{children}</span>;
};

export default PrimaryButton;